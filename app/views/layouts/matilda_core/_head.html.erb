<!DOCTYPE html>
<html>
<head>
  <title><%= MatildaCore.config.global_title %></title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <meta name="viewport" content="width=device-width,initial-scale=1,minimum-scale=1,maximum-scale=1,user-scalable=no">

  <!-- Loader -->
  <!--
  <style>.loader {background-color:#ffffff;position:fixed;top:0;left:0;width:100%;height:100%;z-index:9999999;transition:all 0.3s ease;}.loader.hidden {opacity:0;visibility:hidden;}</style>
  <script>window.addEventListener('DOMContentLoaded', () => {document.querySelector('.loader').classList.add('hidden')})</script>
  -->

  <!-- Custom template -->
  <% if MatildaCore.config.template_head %>
    <%= raw MatildaCore.config.template_head %>
  <% end %>

  <!-- Assets -->
  <%= stylesheet_pack_tag 'matilda' %>
  <%= javascript_pack_tag 'matilda' %>
</head>
<body class="<%= session[:mat_menupreference] ? '' : 'is-menu-closed' %> controller-<%= controller_name %>">
<!--<div class="loader"></div>-->
